# wangsheng_agent AI 开发规则

## 1. 项目信息

- 仓库名称：wangsheng_agent
- Python 包名称：job_agent
- Python 版本：3.11+
- 启动命令：python -m job_agent
- 测试命令：python -m pytest -q
- 当前采用 T001、T002、T003 等任务编号增量开发。

## 2. 开发前必须阅读

每次修改代码前，依次阅读：

1. 本文件 AGENTS.md
2. README.md
3. doc/AI_HANDOFF.md
4. doc/tasks/ 中本次任务对应的 Txxx.md
5. “秋招投递智能体_AI开发文档包”中与本任务相关的设计文档
6. 现有代码和测试

不要无差别读取所有文档，只读取当前任务所需内容。

## 3. 事实来源优先级

出现冲突时，按照以下顺序判断：

1. 当前 Txxx 任务文档和验收标准
2. 项目架构、数据模型及安全规范
3. 现有公开接口和测试
4. AI_HANDOFF.md 中的当前状态
5. README.md 中的概览说明

如果仍然无法确定，不得自行扩大范围，应明确记录假设或请求用户确认。

## 4. 开发原则

- 只实现当前指定的 Txxx，不提前实现后续任务。
- 在现有代码基础上增量开发，不随意重建目录或重写已有模块。
- 保持 job_agent 的公开导入路径向后兼容。
- 领域模型不得直接依赖数据库、LangGraph、Playwright、Streamlit或具体 LLM SDK。
- 数据库、Repository、业务服务、工作流、浏览器自动化和 UI 必须分层。
- 禁止把所有功能集中写入一个 agent.py。
- 新增业务行为必须编写测试。
- 不得为了让测试通过而降低已有校验规则。
- 不得读取、输出或提交 .env 中的密钥。
- 不得执行真实职位投递。
- 不得未经用户确认发送外部请求或执行不可逆操作。
- 不得删除或覆盖与当前任务无关的用户修改。

## 5. 文件职责

- AGENTS.md：AI 开发规则
- README.md：项目介绍、安装和使用方式
- doc/AI_HANDOFF.md：当前开发状态和任务交接
- doc/tasks/Txxx.md：单个任务的需求与验收标准
- job_agent/domain：纯领域模型
- job_agent 中其他目录：根据架构文档逐步建立
- tests：与源码目录对应的测试

## 6. 开发流程

开始任务前：

1. 检查 git status 和当前分支。
2. 阅读本次任务文档。
3. 检查相关代码和已有测试。
4. 说明计划修改的文件和验收方式。
5. 发现工作区有无关修改时停止并报告。

完成任务后：

1. 运行本任务相关测试。
2. 运行完整测试。
3. 运行 python -m compileall -q job_agent tests。
4. 运行 git diff --check。
5. 更新 README.md 和 doc/AI_HANDOFF.md。
6. 汇报修改文件、测试结果、风险和未完成事项。
7. 提供建议的 Git commit message。
8. 未经明确要求，不执行 git commit 或 git push。

## 7. 完成标准

只有同时满足以下条件，任务才算完成：

- 当前任务交付物全部实现
- 验收标准全部满足
- 新增代码有相应测试
- 完整测试没有回归
- 启动入口保持可用
- 文档与实际实现一致
- 没有提交密钥、数据库、缓存或个人信息
